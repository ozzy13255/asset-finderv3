# Asset Finder v3.3.6 deployment

Commit: Improve live approval/removal counters and approval queue stability

Replace the repository `app/`, `supabase/`, `DEPLOYMENT-NEXT.md`, and `README.md` with this build. Supabase migration `20260912_v336_live_queue_counters.sql` is already applied in production.
